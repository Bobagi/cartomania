/**
 * Version tag of the Terms of Use + Privacy Policy currently in force.
 *
 * "Has the user accepted?" always means THIS version. When the legal text
 * materially changes, bump this (date-ordered) tag - every prior acceptance row
 * stops matching, so the agreement gate forces everyone to re-accept. Keep it in
 * sync with the version rendered by the web `/terms` and `/privacy` pages.
 */
export const CURRENT_AGREEMENT_VERSION = '2026-07-23';
